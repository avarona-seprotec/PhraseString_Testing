phrase:
  project_id: "c82889bdf73013f4d77c664e5abdb1ec"      # desde Project Settings > API
  file_format: "html"             					  # ajústalo a tu stack
  push:                                               # ← lo consume el botón IMPORT (repo → Phrase)
    sources:
      - file: "./es/Frutería_index.html"
        params:
          update_translations: false                  # NO pisar traducciones de Phrase con el repo
  pull:                                               # ← lo consume el botón EXPORT (Phrase → repo)
    targets:
      - file: "./en/Frutería_index.html"

